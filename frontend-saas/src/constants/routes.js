/** Pantalla principal tras login o al abrir la raíz del portal (facturación nativa). */
export const RUTA_INICIO_APP = '/facturacion/nueva'

/** Usuarios que solo facturan por PosAgil (sin módulo nativo). */
export const RUTA_INICIO_SOLO_POS = '/dashboard'
