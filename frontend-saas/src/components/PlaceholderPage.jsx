export function PlaceholderPage({ title }) {
  return (
    <div>
      <h1 className="text-xl font-semibold text-gray-800">{title}</h1>
      <p className="mt-2 text-gray-500">
        Aquí migrarás la lógica desde el frontend anterior.
      </p>
    </div>
  )
}
