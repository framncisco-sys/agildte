# Sin espacios antes ni después del =
# Copia a .env y ajusta valores

POSTGRES_DB=sistema_contable
POSTGRES_USER=sistema_user
POSTGRES_PASSWORD=tu_password_seguro

DJANGO_SECRET_KEY=tu_secret_key_produccion
DJANGO_DEBUG=False
DJANGO_ALLOWED_HOSTS=tu-dominio.com,.tu-dominio.com

# Firma DTE: por defecto se usa firmador interno (Python), no hace falta contenedor.
USE_INTERNAL_FIRMADOR=true
# Solo si usas firmador externo (contenedor o terceros):
# USE_INTERNAL_FIRMADOR=false
# FIRMADOR_URL=http://firmador:8113/
